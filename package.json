{
	"name": "internet-message-type",
	"version": "0.0.0",
	"private": true,

	"dependencies": {
		"medium-type": ">= 0.1.337 < 0.2"
	},

	"devDependencies": {
		"internet-message": ">= 0.0.0 < < 0.1.0",
		"mocha": ">= 1.18.2 < 2",
		"must": "< 1"
	}
}
